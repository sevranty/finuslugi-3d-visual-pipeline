# Next Action

Open a Draft PR from `feat/initial-skill-architecture` to `main`, link Issues #1 and #2, record the current HEAD SHA, and keep validation status blocked until the local validator runs on that exact HEAD.
