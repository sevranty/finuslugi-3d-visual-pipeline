# Current HEAD Tracking

The canonical source of the current branch HEAD is GitHub. This file does not pin a SHA because updating it would create a new SHA recursively.

Validation reports and PR review comments must record the exact immutable HEAD SHA at the time they are produced.
