# Branch Policy

- `main` is the stable baseline.
- Feature work uses dedicated branches.
- Initial architecture work uses `feat/initial-skill-architecture`.
- A Draft PR is mandatory before review.
- Direct implementation commits to `main` are prohibited after initialization.
- Rebase, squash, Ready transition, and merge require an explicit lifecycle decision.
