# Merge Gates

Merge is prohibited until:

- PR has been explicitly marked Ready by owner instruction;
- exact Ready HEAD is validated and reviewed;
- no unresolved required review threads remain;
- branch protection and repository settings permit the selected merge method;
- owner sends a separate merge instruction with the intended method.

Passing Draft review alone is insufficient.
