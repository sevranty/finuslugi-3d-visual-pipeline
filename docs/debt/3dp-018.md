# 3DP-018 debt register

## Baseline

- base `main`: `f010b6dcec3c08168b745e36f0ecbd0bb0eba676`
- implementation PR: pending
- release state: `candidate`
- hosted validation on baseline main: not found

## Closed in this task

- local WebFactoryOS orchestration boundary
- compact local task contract
- single repository execution Issue Form
- active project identity and the historical custom `3dp/validation` status context
- deterministic local validation entrypoint matching CI
- workflow concurrency and stale-run cancellation
- explicit release lifecycle states and negative tests
- tool-agnostic usage examples
- factual distinction between local validation and hosted checks

## CI identity history

- `3dp/validation` was the custom commit-status context for the 3DP-018 baseline.
- The current canonical CI identity is the native GitHub Actions check `Validate repository / validate`; the custom status is no longer published.

## Preserved

- runtime sequence
- style contract semantics and versions
- governed visual assets and checksums
- historical commits, tags and Releases
- legacy release target

## Non-actions

- `SECURITY.md` is not added because no confirmed private reporting channel or repository setting is available
- `SUPPORT.md` is not added because the repository Issue Form and README already define the public support entry point
- remote branches are not deleted in this code PR; branch deletion is a separate irreversible metadata action after open-PR and protection checks
- annotated tag and GitHub Release creation are not claimed because the current connector does not expose those operations

## Publication remainder

1. merge the reviewed exact HEAD
2. create annotated tag `v1.0.0` on the approved merge commit
3. run the complete validation set from a clean immutable-tag checkout
4. set manifest state to `tagged-validated`
5. publish the public GitHub Release from checked-in notes
6. set manifest state to `published`
7. merge factual closure evidence

## Evidence

Exact PR HEAD, GitHub Actions run, artifact, review result, merge SHA and final main are recorded in the PR and Issue after verification.
