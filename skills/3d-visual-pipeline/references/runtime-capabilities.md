# Runtime capabilities

Capability vocabulary: `generate`, `reference-conditioned`, `edit`, `mask-edit`, `multi-reference`, `identity-preservation`, `transparent-output`, `upscale`, `exact-dimensions`, and `deliver`. Unknown mandatory capabilities are unsupported.
