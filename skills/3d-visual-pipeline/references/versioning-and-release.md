# Versioning and release

The plugin follows semantic versioning. Release identity must agree across `.codex-plugin/plugin.json`, `CHANGELOG.md`, `../../../release/1.0.0/validation-manifest.json`, release notes, and intended tag `v1.0.0`. Published tags are immutable. A faulty release is deprecated and replaced, never moved.
