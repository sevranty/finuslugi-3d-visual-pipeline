# Scene Specification

The Scene Specification is the source of truth. It records task, mode, subject, metaphor, reference roles, locks, composition, camera, geometry, materials, palette, lighting, background, constraints, runtime requirements, and delivery format. Schema: `../assets/schemas/scene-spec.schema.json`.
