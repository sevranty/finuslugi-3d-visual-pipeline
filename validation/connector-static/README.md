# Connector Static Inspection

This directory records the limitation of the initial GitHub-connector implementation session.

The branch contents were inspected through GitHub file and compare APIs. The Python validator was added but was not executed in that connector-only session because the execution environment could not resolve GitHub to clone or download the branch.

This record is not a passing validation report. A local filesystem run against the exact PR HEAD is still required before the PR may move out of Draft.
