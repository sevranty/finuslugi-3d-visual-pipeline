# Anti-Patterns

Rejected visual fixtures belong here only when a manifest entry links the file to a stable diagnostic code and explains the violated rule. These assets are never production eligible.
