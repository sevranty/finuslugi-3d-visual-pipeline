# Brand Assets

Canonical binary files remain at stable root paths for backward compatibility:

- `../finuslugi-base.png` — white or light backgrounds;
- `../finuslugi-inverted.png` — red or dark backgrounds.

Use only active entries from `../manifest.json`. Logos are post-production assets and never generative references.
