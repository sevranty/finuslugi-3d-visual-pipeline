# Agent contract

Read `TASK.md` before project work. Read `docs/decisions/0002-web-factory-os-orchestration.md` for the WebFactoryOS boundary.

Use `skills/3d-visual-pipeline/SKILL.md` as the runtime source of truth.

Required runtime order: input validation, reference mapping, locks, Scene Specification, asset gate, style selection, runtime routing, generation or edit, visual QA, targeted repair, finalization, and visible delivery.

Do not weaken validators, bypass asset governance, hide fallback behavior, duplicate canonical rules, cross repository write boundaries, or claim completion without a visible deliverable and recorded evidence.